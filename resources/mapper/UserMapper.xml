<?xml version="1.0" encoding="UTF-8" ?>
<mapper namespace="com.test.entity.User">
    <select id="getUserInfo" parameterType="java.lang.Long" resultType="com.test.entity.User">
        select id, name
        from user
        where id = ?
    </select>
</mapper>